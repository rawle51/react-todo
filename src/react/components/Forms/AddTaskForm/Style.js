import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  margin-bottom: 12px;

  > div {
    flex: 1;
  }

  > svg {
    margin-left: 48px;
  }

  input {
    height: 54px;
  }
`;
